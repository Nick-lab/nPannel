<!DOCTYPE html>
<html>
    <head>
        {{!-- Global Resources --}}
        {{#each styles}}
            <link rel="stylesheet" href="{{src}}">
        {{/each}}
        {{!-- Page Resources --}}
        <script src="/socket.io/socket.io.js"></script>
        {{#each scripts}}
            <script src="{{src}}" {{#if defer}}async defer{{/if}}></script>
        {{/each}}
        {{!-- Meta --}}
        <title>{{ page.title }}</title>
        
    </head>
    <body class="body">
        {{> header}}
        {{{body}}}
    </body>
</html>